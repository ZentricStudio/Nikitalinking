# Shared Team Memory

This file is loaded into every linked Claude (via `scripts/install.sh`). Anything
here is knowledge the whole group wants every Claude to have — conventions,
preferences, background, gotchas.

Keep it tidy. Short, clear rules beat long paragraphs.

## Conventions

- (e.g. "We use pnpm, not npm.")
- (e.g. "Default to TypeScript for new code.")

## Things every Claude should know

- (e.g. "Our main project lives at github.com/ZentricStudio/…")

## Preferences

- (e.g. "Be concise. Prefer bullet points over essays.")

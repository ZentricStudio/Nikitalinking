# Shared agents

Drop shared subagent definitions here as `<agent-name>.md` files. They'll be
available to everyone who installs the `shared-library` plugin.

See the Claude Code docs for the agent frontmatter format.
